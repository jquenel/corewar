void corewar_live(t_sen *arena, t_arg *arg1, t_arg *arg2, t_arg *arg3)
{
	t_bushi *player;
	int		nb_player;

	if (arg1 == NULL || arg2 != NULL || arg3 != NULL || arg1->type != 2)
		return ;
	nb_player = (int)arg->value
	ft_printf("le joueur %d(%s) est en vie\n", player->pnum, player->name);
}
